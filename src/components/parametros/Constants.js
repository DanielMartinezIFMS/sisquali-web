const URL = '/';
const REST = '/rest';
const IMAGE = '/rest/image';
const FILE = '/rest/file';
const FILESIZEMAX = 1024 * 1024 * 10;
export default {
  url: URL,
  rest: REST,
  image: IMAGE,
  file: FILE,
  FILESIZEMAX: FILESIZEMAX
};
